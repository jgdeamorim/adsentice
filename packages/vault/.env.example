# adsentice/vault — creds do cofre (NUNCA commitar o .env real · está no .gitignore)

# Cloudflare R2 (o blob cru · egress zero)
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=adsentice-vault

# Supabase (a série · query_vault) — SERVICE ROLE (o backend escreve · nunca o anon key)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
