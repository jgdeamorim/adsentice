{
  "name": "@adsentice/vault",
  "version": "0.0.1",
  "description": "O cofre durável do adsentice — write-ahead (ouro DataForSEO antes do índice)",
  "type": "module",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "test": "node --import tsx --test src/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@noble/hashes": "^1.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
