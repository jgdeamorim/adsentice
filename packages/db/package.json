{
  "name": "@adsentice/db",
  "version": "0.1.0",
  "private": true,
  "description": "adsentice unified database layer — R2 + Supabase + Qdrant + cache. Dev in-memory, prod cloud.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "node --import tsx --test src/db.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.0.0",
    "@supabase/supabase-js": "^2.0.0",
    "@noble/hashes": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "tsx": "^4.0.0"
  }
}
